# led-ticker configuration — longboi (512×64 canvas)
#
# 4× Muen P2 128×64 panels chained horizontally = 512×64 physical.
# Driven by a Raspberry Pi 5 through an Adafruit RGB Matrix Bonnet (3211).
# No pixel_mapper_config needed — single horizontal chain_length, left-to-right.
#
# These panels use 1/32 scan with an FM6126A (or compatible) driver IC.
# panel_type = "FM6126A" sends the required initialization sequence at startup;
# without it the driver powers up in a bad state and the bottom half of each
# panel renders mirrored or garbled.
#
# default_scale = 4 means all widgets draw at a logical 128×16 canvas and the
# ScaledCanvas wrapper expands every pixel to a 4×4 real block on the 64-tall
# panel. Same layout model as bigsign; hires fonts, hires emoji, and hires
# sprite transitions (pokeball, baseball, nyancat) all activate automatically.

[display]
rows = 64
cols = 128
chain_length = 4
brightness = 60
default_scale = 4
hardware_mapping = "adafruit-hat"
panel_type = "FM6126A"
# Muen P2 panels wire HUB75 G-pin → Red LED, R-pin → Blue LED, B-pin → Green LED.
led_rgb_sequence = "BRG"

# Pi 5 RP1 RIO backend (faster refresh, slightly more CPU than PIO).
# rp1_rio = 1 requires row_address_type = 0 (default) — do not set row_address_type = 1.
# Raise gpio_slowdown to 4–5 if flicker persists.
gpio_slowdown = 5
rp1_rio = 1
pwm_bits = 7     # 7-bit PWM: tuned for camera; default is 11
limit_refresh_rate_hz = 100   # caps refresh to stabilise image on camera

[title]
delay = 5

[transitions]
default = "push_left"
duration = 0.5
easing = "ease_out"
between_sections = {type = "nyancat_alternating", duration = 4.0}

# --------------------------------------------------------------------------
# Playlist
# --------------------------------------------------------------------------

# --- Messages ---

[[playlist.section]]
mode = "swap"
transition = "pacman_alternating"
transition_duration = 3.0
continuous_scroll = true
loop_count = 1

[playlist.section.title]
type = "message"
text = ":bunny: #DevOps News :taco:"
font = "Inter-Regular"
font_size = 32
font_threshold = 80
font_color = "random"

[[playlist.section.widget]]
type = "message"
text = "Check out Datadog MCP!"
font = "Inter-Regular"
font_size = 32
font_threshold = 80

[[playlist.section.widget]]
type = "message"
text = "Talk to #team-foundations for all your DevOps needs!"
font = "Inter-Regular"
font_size = 32
font_threshold = 80

[[playlist.section.widget]]
type = "message"
text = "Ask me about eden review apps! :taco:"
font = "Inter-Regular"
font_size = 32
font_threshold = 80

# --- Weather ---
# Requires WEATHERAPI_KEY in .env (https://www.weatherapi.com/).

[[playlist.section]]
mode = "swap"
transition = "push_alternating"
hold_time = 6
loop_count = 2

[playlist.section.title]
type = "message"
text = "Weather"
font = "Inter-Regular"
font_size = 32
font_threshold = 80
font_color = "random"

[[playlist.section.widget]]
type = "weather"
text = "New York"
location = "New York"
units = "imperial"
font = "Inter-Regular"
font_size = 32
font_threshold = 80

[[playlist.section.widget]]
type = "weather"
text = "Philadelphia"
location = "Philadelphia"
units = "imperial"
font = "Inter-Regular"
font_size = 32
font_threshold = 80

[[playlist.section.widget]]
type = "weather"
text = "Milwaukee"
location = "Milwaukee"
units = "imperial"
font = "Inter-Regular"
font_size = 32
font_threshold = 80

[[playlist.section.widget]]
type = "weather"
text = "Minneapolis"
location = "Minneapolis"
units = "imperial"
font = "Inter-Regular"
font_size = 32
font_threshold = 80

[[playlist.section.widget]]
type = "weather"
text = "Washington DC"
location = "Washington DC"
units = "imperial"
font = "Inter-Regular"
font_size = 32
font_threshold = 80

# --- Countdowns ---

[[playlist.section]]
mode = "swap"
transition = "wipe_alternating"
loop_count = 1

[playlist.section.title]
type = "message"
text = "Count Downs"
font = "Inter-Regular"
font_size = 32
font_threshold = 80
font_color = "random"

[[playlist.section.widget]]
type = "countdown"
text = "4th of July"
countdown_date = 2026-07-04
font = "Inter-Regular"
font_size = 32
font_threshold = 80

[[playlist.section.widget]]
type = "countdown"
text = "Halloween"
countdown_date = 2026-10-31
font = "Inter-Regular"
font_size = 32
font_threshold = 80

[[playlist.section.widget]]
type = "countdown"
text = "Thanksgiving"
countdown_date = 2026-11-26
font = "Inter-Regular"
font_size = 32
font_threshold = 80

[[playlist.section.widget]]
type = "countdown"
text = "Christmas"
countdown_date = 2026-12-25
font = "Inter-Regular"
font_size = 32
font_threshold = 80

[[playlist.section.widget]]
type = "countdown"
text = "New Years"
countdown_date = 2027-01-01
font = "Inter-Regular"
font_size = 32
font_threshold = 80

# --- Nintendo Life RSS ---
# Hires pokeball_alternating auto-activates at default_scale = 4.

[[playlist.section]]
mode = "swap"
transition = "pokeball_alternating"
transition_duration = 2.0
continuous_scroll = true
loop_count = 1

[playlist.section.title]
type = "message"
text = "Nintendo Life"
font = "Inter-Regular"
font_size = 32
font_threshold = 80
font_color = "random"

[[playlist.section.widget]]
type = "rss_feed"
feed_url = "https://www.nintendolife.com/feeds/news"
update_interval = 3000
font = "Inter-Regular"
font_size = 32
font_threshold = 80

# --- MLB Scores ---
# Hires baseball_alternating auto-activates at default_scale = 4.

[[playlist.section]]
mode = "swap"
transition = "baseball_alternating"
transition_duration = 2.0
transition_fps = 40
hold_time = 6
loop_count = 2

[playlist.section.title]
type = "message"
text = ":baseball: Oh! That's Baseball!! :baseball:"
font = "Inter-Regular"
font_size = 32
font_threshold = 80
font_color = "random"

[[playlist.section.widget]]
type = "mlb"
team = "PHI"
timezone = "America/New_York"
font = "Inter-Regular"
font_size = 32
font_threshold = 80

[[playlist.section.widget]]
type = "mlb"
team = "NYM"
timezone = "America/New_York"
font = "Inter-Regular"
font_size = 32
font_threshold = 80

# --- MLB Standings ---
# Hires baseball_alternating auto-activates at default_scale = 4.

[[playlist.section]]
mode = "swap"
transition = "baseball_alternating"
transition_duration = 2.0
transition_fps = 40
hold_time = 6
loop_count = 1

[playlist.section.title]
type = "message"
text = ":baseball: MLB Standings :baseball:"
font = "Inter-Regular"
font_size = 32
font_threshold = 80
font_color = "random"

[[playlist.section.widget]]
type = "mlb_standings"
teams = ["PHI", "NYM"]
title = "MLB Standings"
top_n = 3
timezone = "America/New_York"
font = "Inter-Regular"
font_size = 32
font_threshold = 80

# --- Pool Temps ---
# Requires INFLUXDB_TOKEN (and URL/ORG/BUCKET) in .env.

[[playlist.section]]
mode = "swap"
transition = "wipe_alternating"
hold_time = 6
loop_count = 1

[playlist.section.title]
type = "message"
text = "Pool Temps"
font = "Inter-Regular"
font_size = 32
font_threshold = 80
font_color = "random"

# The pool widget is now the `led-ticker-pool` plugin (type = "pool.monitor").
# Install it for this config to work: pip install led-ticker-pool
# (the repo's Dockerfile already installs it; entry-point auto-registers it).
[[playlist.section.widget]]
type = "pool.monitor"
title = "POOL TEMPS"
units = "imperial"
update_interval = 60
stale_after = 14400  # 4 hours
font = "Inter-Regular"
font_size = 32
font_threshold = 80
label_color = [130, 220, 255]

# --- Anime News Network ---

[[playlist.section]]
mode = "swap"
transition = "sailor_moon_alternating"
transition_duration = 2.0
continuous_scroll = true
loop_count = 1

[playlist.section.title]
type = "message"
text = "Anime News Network"
font = "Inter-Regular"
font_size = 32
font_threshold = 80
font_color = "random"

[[playlist.section.widget]]
type = "rss_feed"
feed_url = "https://www.animenewsnetwork.com/all/rss.xml?ann-edition=us"
update_interval = 3000
font = "Inter-Regular"
font_size = 32
font_threshold = 80

# --- Busy light (HTTP trigger) ---
# A persistent dot in the top-left corner, flipped on/off by an HTTP call over
# the LAN (e.g. a work Mac hotkey or a macOS Focus automation). longboi's
# compose uses network_mode: host, so the listener on :8080 is already reachable
# at the Pi's address — no Docker port mapping needed. Toggle it with:
#   curl "http://longboi:8080/busy?state=on&token=changeme"
#   curl "http://longboi:8080/busy?state=off&token=changeme"
#   curl "http://longboi:8080/busy?state=on&ttl=3600&token=changeme"  # auto-clear in 1h
# Docs: https://docs.ledticker.dev/concepts/busy-light/
# [busy_light]
# enabled = true
# source = "http"
# http_host = "0.0.0.0"
# http_port = 8080
# token = "changeme"
# corner = "top_left"
# size = 10            # physical px; ~8-10 on the 64-tall longboi
# color = [255, 0, 0]
# ttl_seconds = 0      # default auto-clear; 0 = stay on until explicit off
