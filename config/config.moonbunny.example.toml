# Moon Bunny Aerial — store-window display config (Pi 5 bigsign).
#
# Hung in a building window at street level, viewable from across the street.
# 8× P3 32×64 panels in 2×4 vertical-serpentine = 256×64 logical.
# Brand: Beloved Sans + Inter, lavender / pink / sky / cream palette.
# Site: https://www.moonbunnyaerial.com  •  IG: @moonbunnyaerial
#
# Far-viewing distance ⇒ all text uses hires fonts at 24-32px (no BDF).

# ---------------------------------------------------------------------------
# Display + RP1 performance tuning (matches config.bigsign.example.toml)
# ---------------------------------------------------------------------------

[display]
rows = 32
cols = 64
chain = 8
parallel = 1
pixel_mapper = "Remap:256,64|192,32n|192,0n|128,32n|128,0n|64,32n|64,0n|0,32n|0,0n"

brightness = 60
slowdown_gpio = 3
gpio_mapping = "adafruit-hat"
default_scale = 4

pwm_bits = 8
show_refresh = true
rp1_rio = 1

[title]
delay = 5

# Playful, animated transitions matching the warm/empowering brand voice.
# `wipe_alternating` cycles through directions using brand-colored sweep
# lines; `pokeball_alternating` is the section divider — a fun cue that
# something new is starting.
[transitions]
default = "wipe_alternating"
duration = 0.8
easing = "ease_in_out"
between_sections = "pokeball_alternating"
# Brand-tinted wipe color pool: lavender, pink, sky, cream
transition_colors = [[189, 169, 234], [255, 176, 240], [124, 173, 232], [254, 255, 204]]

# ---------------------------------------------------------------------------
# Brand color palette (from brand_guide.png)
#
#   lavender_primary = [189, 169, 234]   BDA9EA — the brand's signature lavender
#   pink             = [255, 176, 240]   FFB0F0 — soft pink accent
#   sky              = [124, 173, 232]   7CADE8 — cool blue accent
#   navy             = [10,  74, 137]    0A4A89 — deep blue (high contrast)
#   teal             = [11, 126, 135]    0B7E87 — secondary depth
#   cream            = [254, 255, 204]   FEFFCC — warm white, body text
# ---------------------------------------------------------------------------

# ---------------------------------------------------------------------------
# Section 1 — Logo hero (full-panel banner with rainbow border)
#
# Uses logo_banner_color.png — already laid out as horizontal lockup:
# moon icon + "MOON BUNNY AERIAL" + "AERIAL FOR EVERYBODY" tagline.
# letterbox fit means tiny black bars top/bottom; rainbow border frames it.
# ---------------------------------------------------------------------------

[[playlist.section]]
mode = "swap"
loop_count = 1
hold_time = 6.0
transition = "dissolve"
transition_duration = 1.0

[[playlist.section.widget]]
type = "image"
path = "assets/logo_banner_color.png"
fit = "letterbox"
hold_seconds = 6.0
border = {style = "rainbow", thickness = 2, speed = 3, char_offset = 5}

# ---------------------------------------------------------------------------
# Section 2 — "MOONBUNNY" double rainbow hold
#
# Static hold at large size with rainbow per-char text AND rainbow border
# chasing around the perimeter — two rainbows. The :moon: + :bunny:
# emoji slugs render the hires moon and bunny sprites at native scale.
# ---------------------------------------------------------------------------

[[playlist.section]]
mode = "swap"
loop_count = 1
hold_time = 5.0
transition = "dissolve"
transition_duration = 0.8

[[playlist.section.widget]]
type = "message"
text = ":moon: MOONBUNNY :bunny:"
font = "beloved-sans-bold"
font_size = 26
font_threshold = 80
font_color = "rainbow"
border = {style = "rainbow", thickness = 2, speed = 4, char_offset = 6}

# ---------------------------------------------------------------------------
# Section 3 — "Find Your" combined scroll
#
# logo_moon_only.png pillarboxed on the left; the three brand phrases
# walk BEHIND the moon (text_align="scroll" — image painted on top with
# skip-black, so text shows through the transparent regions of the
# moon's halo and disappears behind the moon body itself). Per-char
# lavender → pink gradient gives the ombre look.
# ---------------------------------------------------------------------------

[[playlist.section]]
mode = "swap"
loop_count = 1
hold_time = 0.5

[[playlist.section.widget]]
type = "image"
path = "assets/logo_moon_only.png"
fit = "pillarbox"
image_align = "left"
text = "Find Your Strength  *  Find Your Community  *  Aerial for Everybody"
text_align = "scroll"
scroll_speed_ms = 50
hold_seconds = 0.05   # min: text_loops drives the real duration
text_loops = 1
font = "beloved-sans-bold"
font_size = 28
font_threshold = 80
font_color = {style = "gradient", from = [189, 169, 234], to = [255, 176, 240]}

# ---------------------------------------------------------------------------
# Section 3 — Kids Summer Camps  (with Pikachu wave + rainbow border)
#
# pika_wave_transparent.gif (220×220) pillarboxed on the left at scale=4
# native res; scrolling text "KIDS SUMMER CAMPS — NOW ENROLLING" walks
# across the panel with the rainbow border chasing around the perimeter.
# Transition pinned to pokeball_alternating per the user's request.
# ---------------------------------------------------------------------------

[[playlist.section]]
mode = "swap"
loop_count = 1
hold_time = 8.0
transition = "pokeball_alternating"
transition_duration = 1.5

[[playlist.section.widget]]
type = "gif"
path = "assets/pika_wave_transparent.gif"
fit = "pillarbox"
image_align = "left"
text = "KIDS SUMMER CAMPS  *  NOW ENROLLING  *  ALL AGES WELCOME"
text_align = "scroll"
scroll_speed_ms = 30
play_count = 0   # play through hold_time
font = "beloved-sans-bold"
font_size = 28
font_threshold = 80
font_color = [255, 176, 240]   # pink — pops against pikachu yellow
border = {style = "rainbow", thickness = 2, speed = 3, char_offset = 5}

# ---------------------------------------------------------------------------
# Section 4 — K-pop dance class (promo)
#
# kpop-dance.webp (540×400 ≈ 1.35:1) pillarboxed on the left. Inter-Bold
# at 26 reads cleanly across the street (more open shapes than Beloved
# Sans at this size). text_align="scroll_over" paints the video first
# then the text — so the marquee walks IN FRONT of the dance footage,
# guaranteed visible regardless of frame brightness (kpop-dance.webp
# has no transparent regions).
# ---------------------------------------------------------------------------

[[playlist.section]]
mode = "swap"
loop_count = 1
hold_time = 6.0
transition = "nyancat_alternating"
transition_duration = 2.0

[[playlist.section.widget]]
type = "gif"
path = "assets/kpop-dance.webp"
fit = "pillarbox"
image_align = "left"
text = "K-POP DANCE CLASS  *  NOW OPEN  *  ALL LEVELS"
text_align = "scroll_over"
scroll_speed_ms = 50
play_count = 0   # play through hold_time
font = "Inter-Bold"
font_size = 26
font_threshold = 128
font_color = [254, 255, 204]   # cream — high contrast on the dance video

# ---------------------------------------------------------------------------
# Section 5 — Classes we offer
#
# One discipline per beat. Cycles through: Aerial Silks, Lyra, Flow Props,
# Ballet, Dance, All Levels — using brand color rotation.
# ---------------------------------------------------------------------------

[[playlist.section]]
mode = "swap"
loop_count = 1
hold_time = 2.5

[playlist.section.title]
type = "message"
text = "Classes"
font = "beloved-sans-bold"
font_size = 24
font_threshold = 80
font_color = [189, 169, 234]

[[playlist.section.widget]]
type = "message"
text = "Aerial Silks"
font = "beloved-sans-bold"
font_size = 30
font_threshold = 80
font_color = [189, 169, 234]   # lavender

[[playlist.section.widget]]
type = "message"
text = "Lyra"
font = "beloved-sans-bold"
font_size = 30
font_threshold = 80
font_color = [255, 176, 240]   # pink

[[playlist.section.widget]]
type = "message"
text = "Flow Props"
font = "beloved-sans-bold"
font_size = 30
font_threshold = 80
font_color = [124, 173, 232]   # sky

[[playlist.section.widget]]
type = "message"
text = "Ballet"
font = "beloved-sans-bold"
font_size = 30
font_threshold = 80
font_color = [254, 255, 204]   # cream

[[playlist.section.widget]]
type = "message"
text = "Dance"
font = "beloved-sans-bold"
font_size = 30
font_threshold = 80
font_color = [11, 126, 135]    # teal

[[playlist.section.widget]]
type = "message"
text = "All Levels Welcome"
font = "beloved-sans-bold"
font_size = 28
font_threshold = 80
font_color = [189, 169, 234]   # lavender

# ---------------------------------------------------------------------------
# Section 6 — Welcoming / inclusion (combined scroll)
#
# All three phrases on one continuous marquee with a per-char
# pink → cream → lavender ColorCycle so the whole line cycles
# through the brand palette as it scrolls.
# ---------------------------------------------------------------------------

[[playlist.section]]
mode = "swap"
loop_count = 1
hold_time = 0.5
transition = "sailor_moon_alternating"
transition_duration = 2.0

[[playlist.section.widget]]
type = "message"
text = "Every Body Friendly  *  Beginner Friendly  *  Meet You Where You Are"
font = "beloved-sans-bold"
font_size = 28
font_threshold = 80
font_color = "color_cycle"

# ---------------------------------------------------------------------------
# Section 7 — IG handle two-row (storefront-window layout)
#
# scale=2 ⇒ 128 logical px wide so "@moonbunnyaerial" fits on the held
# top row. content_height=20 gives 2 rows of breathing space between
# the two text bands. Inter-Bold for the handle (matches the IG look),
# Inter-Regular for the bottom promo line (lighter weight reads as
# secondary copy from across the street).
# ---------------------------------------------------------------------------

[[playlist.section]]
mode = "swap"
scale = 2
content_height = 24
loop_count = 1
hold_time = 4.0
transition = "dissolve"
transition_duration = 1.0

[[playlist.section.widget]]
type = "two_row"
top_text = ":instagram: @moonbunnyaerial"
top_color = [225, 48, 108]   # IG brand magenta
top_align = "center"
top_font = "Inter-Bold"
top_font_size = 18
top_font_threshold = 128
bottom_text = "Now booking spring classes — all levels welcome!"
bottom_color = [254, 255, 204]
bottom_align = "left"
bottom_font = "Inter-Regular"
bottom_font_size = 16
bottom_font_threshold = 128

[[playlist.section.widget]]
type = "two_row"
top_text = ":instagram: @moonbunnyaerial"
top_color = [225, 48, 108]
top_align = "center"
top_font = "Inter-Bold"
top_font_size = 18
top_font_threshold = 128
bottom_text = "Kids Summer Camps now enrolling — sign up today!"
bottom_color = [255, 176, 240]
bottom_align = "left"
bottom_font = "Inter-Regular"
bottom_font_size = 16
bottom_font_threshold = 128

[[playlist.section.widget]]
type = "two_row"
top_text = ":instagram: @moonbunnyaerial"
top_color = [225, 48, 108]
top_align = "center"
top_font = "Inter-Bold"
top_font_size = 18
top_font_threshold = 128
bottom_text = "Aerial silks * lyra * flow props * ballet * dance"
bottom_color = [189, 169, 234]
bottom_align = "left"
bottom_font = "Inter-Regular"
bottom_font_size = 16
bottom_font_threshold = 128

[[playlist.section.widget]]
type = "two_row"
top_text = ":email: info@moonbunnyaerial.com"
top_color = [254, 255, 204]
top_align = "center"
top_font = "Inter-Bold"
top_font_size = 18
top_font_threshold = 128
bottom_text = "Questions? Send us a note — we love hearing from you!"
bottom_color = [124, 173, 232]
bottom_align = "left"
bottom_font = "Inter-Regular"
bottom_font_size = 16
bottom_font_threshold = 128

# ---------------------------------------------------------------------------
# Section 8 — Connect (combined scroll: site + email + IG handle)
# ---------------------------------------------------------------------------

[[playlist.section]]
mode = "swap"
loop_count = 1
hold_time = 0.5

[[playlist.section.widget]]
type = "message"
text = ":instagram: @moonbunnyaerial  *  :email: info@moonbunnyaerial.com  *  moonbunnyaerial.com"
font = "beloved-sans-bold"
font_size = 26
font_threshold = 80
font_color = "color_cycle"
