# Presentation effects smoke test — Pi 5 bigsign.
#
# Validates the new font_color (color provider) + animation knobs
# on bigsign. Replaces the legacy presentation = "..." wrapper.
#
# Sections (the first two are what's NEW on this branch):
#   1. rainbow + emoji body              — per-char rainbow sweeps across :slug: boundaries
#   2. rainbow title + body              — title color = "rainbow" (provider on title)
#
#   3. animation = "typewriter"          — chars type out left-to-right (BDF)
#   4. font_color = "color_cycle"        — whole-string hue rotation (BDF)
#   5. font_color = "rainbow"            — per-char rainbow showcase (BDF)
#   6. font_color = {style="gradient",..} — left-to-right red→blue gradient (BDF)
#   7. font_color = "rainbow" + Countdown — different widget type (BDF)
#   8. font_color = "color_cycle" + Weather — live-data text recoloring (BDF)
#   9. HiresFont + rainbow                — Inter-Bold per-char rainbow
#  10. HiresFont + gradient               — Inter-Regular left-to-right gradient
#  11. HiresFont + typewriter             — Inter-Bold types out one char at a time
#
# §1 (rainbow + emoji): the rainbow sweeps continuously across
# emoji boundaries — each `:taco:` renders as the taco sprite, the
# letters between/around it get their own rainbow hues with
# char_index advancing across emoji segments without resetting.
#
# §2 (title rainbow): titles accept the same `color` shapes as widget
# `font_color` — string shorthand (`"rainbow"`, `"color_cycle"`),
# inline tables (`{style="gradient", from=[...], to=[...]}`), or the
# legacy `[r, g, b]` constant.

[display]
rows = 32
cols = 64
chain = 8
parallel = 1
pixel_mapper = "Remap:256,64|192,32n|192,0n|128,32n|128,0n|64,32n|64,0n|0,32n|0,0n"

brightness = 60
slowdown_gpio = 3
gpio_mapping = "adafruit-hat"
default_scale = 4

pwm_bits = 8
rp1_rio = 1

[title]
delay = 0.5

[transitions]
default = "cut"
duration = 0.2
easing = "linear"
between_sections = "cut"

# 1. rainbow + emoji body — per-char sweep continues across :slug: boundaries
[[playlist.section]]
mode = "swap"
hold_time = 8.0
loop_count = 1
transition = "cut"

[playlist.section.title]
type = "message"
text = "1: rainbow + emoji"
color = [200, 220, 255]

[[playlist.section.widget]]
type = "message"
text = ":taco: HOT TACOS :taco:"
font_color = "rainbow"

# 2. rainbow title + body — color provider on the title widget itself
[[playlist.section]]
mode = "swap"
hold_time = 8.0
loop_count = 1
transition = "cut"

[playlist.section.title]
type = "message"
text = "2: TITLE RAINBOW"
color = "rainbow"

[[playlist.section.widget]]
type = "message"
text = "title above is animated"
font_color = [255, 220, 50]

# 3. typewriter
[[playlist.section]]
mode = "swap"
hold_time = 8.0
loop_count = 1
transition = "cut"

[playlist.section.title]
type = "message"
text = "3: typewriter"
color = [200, 220, 255]

[[playlist.section.widget]]
type = "message"
text = "TYPING NOW"
font_color = [255, 220, 50]
animation = "typewriter"

# 4. color_cycle
[[playlist.section]]
mode = "swap"
hold_time = 8.0
loop_count = 1
transition = "cut"

[playlist.section.title]
type = "message"
text = "4: color_cycle"
color = [200, 220, 255]

[[playlist.section.widget]]
type = "message"
text = "WHOLE TEXT CYCLES HUE"
font_color = "color_cycle"

# 5. rainbow
[[playlist.section]]
mode = "swap"
hold_time = 8.0
loop_count = 1
transition = "cut"

[playlist.section.title]
type = "message"
text = "5: rainbow"
color = [200, 220, 255]

[[playlist.section.widget]]
type = "message"
text = "RAINBOW PER-CHARACTER"
font_color = "rainbow"

# 6. gradient — left-to-right color interpolation
[[playlist.section]]
mode = "swap"
hold_time = 8.0
loop_count = 1
transition = "cut"

[playlist.section.title]
type = "message"
text = "6: gradient"
color = [200, 220, 255]

[[playlist.section.widget]]
type = "message"
text = "RED TO BLUE GRADIENT"
font_color = {style = "gradient", from = [255, 0, 0], to = [0, 100, 255]}

# 7. rainbow + countdown
[[playlist.section]]
mode = "swap"
hold_time = 8.0
loop_count = 1
transition = "cut"

[playlist.section.title]
type = "message"
text = "7: rainbow + countdown"
color = [200, 220, 255]

[[playlist.section.widget]]
type = "countdown"
message = "Days to NYE"
countdown_date = 2027-01-01
font_color = "rainbow"

# 8. color_cycle + weather
[[playlist.section]]
mode = "swap"
hold_time = 8.0
loop_count = 1
transition = "cut"

[playlist.section.title]
type = "message"
text = "8: color_cycle + weather"
color = [200, 220, 255]

[[playlist.section.widget]]
type = "weather"
message = "Brooklyn"
location = "Brooklyn"
units = "imperial"
font_color = "color_cycle"

# 9. HiresFont + rainbow — per-char hires rendering
[[playlist.section]]
mode = "swap"
hold_time = 8.0
loop_count = 1
transition = "cut"

[playlist.section.title]
type = "message"
text = "9: hires + rainbow"
color = [200, 220, 255]

[[playlist.section.widget]]
type = "message"
text = "INTER BOLD RAINBOW"
font = "Inter-Bold"
font_size = 24
font_color = "rainbow"

# 10. HiresFont + gradient — per-char hires rendering
[[playlist.section]]
mode = "swap"
hold_time = 8.0
loop_count = 1
transition = "cut"

[playlist.section.title]
type = "message"
text = "10: hires + gradient"
color = [200, 220, 255]

[[playlist.section.widget]]
type = "message"
text = "GREEN TO MAGENTA"
font = "Inter-Regular"
font_size = 24
font_color = {style = "gradient", from = [50, 220, 100], to = [255, 50, 200]}

# 11. HiresFont + typewriter — animation + hires render path
[[playlist.section]]
mode = "swap"
hold_time = 8.0
loop_count = 1
transition = "cut"

[playlist.section.title]
type = "message"
text = "11: hires + typewriter"
color = [200, 220, 255]

[[playlist.section.widget]]
type = "message"
text = "TYPED IN INTER"
font = "Inter-Bold"
font_size = 24
font_color = [80, 200, 255]
animation = "typewriter"
