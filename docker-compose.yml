version: "3.8"

services:
  ticker: &default
    build:
      context: .
      dockerfile: Dockerfile
    devices:
      - "/dev/mem:/dev/mem"
    privileged: true
    restart: always
    env_file:
      - ./settings.env
    volumes:
      - .:/code
