---
title: Available plugins
description: First-party and community led-ticker plugins, and the repositories where they live.
---

Plugins live in their own repositories. To use one, add it to `config/requirements-plugins.txt` and rebuild — see [Plugins](/plugins/) for the install flow. Each plugin's own README documents its options, layouts, and setup.

## Widgets

### [led-ticker-pool](https://github.com/JamesAwesome/led-ticker-pool)

Pool water-temperature widget (`type = "pool.monitor"`), backed by an InfluxDB v2 server. Cycles a title card, today's temperature, 7-day mean, and season hi/lo with zone coloring; supports `ticker` and `two_row` layouts. Full docs, options, and screenshots in the [repo README](https://github.com/JamesAwesome/led-ticker-pool#readme).

Add to your `config/requirements-plugins.txt`:

```text
git+https://github.com/JamesAwesome/led-ticker-pool.git@main
```

### [led-ticker-baseball](https://github.com/JamesAwesome/led-ticker-baseball)

MLB scores (`type = "baseball.scores"`) and standings (`type = "baseball.standings"`) widgets, backed by MLB's free StatsAPI (no key required). Also ships the `baseball.roll` / `baseball.roll_reverse` / `baseball.roll_alternating` rolling-ball sprite transition and the `:baseball.ball:` emoji. Full docs, options, and screenshots in the [repo README](https://github.com/JamesAwesome/led-ticker-baseball#readme).

Add to your `config/requirements-plugins.txt`:

```text
git+https://github.com/JamesAwesome/led-ticker-baseball.git@main
```

### [led-ticker-crypto](https://github.com/JamesAwesome/led-ticker-crypto)

CoinGecko price ticker (`type = "crypto.coingecko"`). Displays live spot prices for any coin/currency pair supported by the [CoinGecko API](https://www.coingecko.com/en/api) (no API key required for the free tier). Full docs, options, and setup in the [repo README](https://github.com/JamesAwesome/led-ticker-crypto#readme).

Add to your `config/requirements-plugins.txt`:

```text
git+https://github.com/JamesAwesome/led-ticker-crypto.git@main
```

---

## Add your plugin

Built one? It belongs here. Open a PR that adds an entry above with:

- a one-line description and the link to your repo,
- a repo `README` with install steps **and a minimal `config.toml` widget block** (showing `type =`, the required keys, and their defaults) so users can copy-paste — see [led-ticker-pool](https://github.com/JamesAwesome/led-ticker-pool) for the shape.

Every working plugin belongs here — including personal-use ones. Listing it grows what led-ticker can do and helps the next person. The [authoring guide](/plugins/authoring/01-scaffold/) takes you from nothing to a shippable widget.
