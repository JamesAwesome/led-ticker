---
title: Special transitions
description: "Catch-all for transitions that aren't push, wipe, or sprite — cut, color_flash, dissolve, split, scroll."
---

import DemoGif from "../../../components/DemoGif.astro";
import OptionsTable from "../../../components/OptionsTable.astro";
import RelatedPages from "../../../components/RelatedPages.astro";

The special family covers transitions that don't fit the push / wipe / sprite shapes. Most are full-canvas effects (`color_flash`, `dissolve`, `split`) or single-frame cuts; `scroll` is the odd one out — a continuous-scroll bridge that connects two widgets with a bullet separator instead of pausing for a transition.

<DemoGif
  src="/demos/transitions-dissolve.gif"
  caption="dissolve — random pixel scatter (TV-static effect)"
/>

<DemoGif
  src="/demos/transitions-split.gif"
  caption="split — center-outward expanding black band with magenta edges"
/>

<DemoGif
  src="/demos/transitions-color_flash.gif"
  caption="color_flash — full-canvas flash, white / cyan / magenta via transition_color"
/>

<DemoGif
  src="/demos/transitions-scroll.gif"
  caption="scroll — seamless continuous scroll with a bullet separator"
/>

The scroll separator is a small white dot by default. Recolor it (e.g. to tone
it down on bright content) with `separator_color` on the transition table —
`transition = { type = "scroll", separator_color = [80, 80, 80] }` — accepting
the same values as a widget `font_color`, including `"rainbow"`.

## Variants

<OptionsTable source="transitions/special" />

<RelatedPages slugs={["transitions/push", "transitions/wipe", "transitions/sprite"]} />
