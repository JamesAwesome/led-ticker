---
title: gif widget
description: Plays animated gifs/webp at native panel resolution, with optional text overlay.
---

import DemoGif from "../../../components/DemoGif.astro";
import TomlExample from "../../../components/TomlExample.astro";
import OptionsTable from "../../../components/OptionsTable.astro";
import RelatedPages from "../../../components/RelatedPages.astro";

The `gif` widget plays animated images at native panel resolution — each LED maps to one pixel in the source, not a scaled block. Format-agnostic: anything Pillow opens with `n_frames` and `seek()` works (`.gif`, animated `.webp`, `.apng`, multi-frame `.tiff`). Per-frame durations come from the file's own metadata.

Set `text` and it appears on the panel alongside the animation. When the text is longer than the canvas, set `text_align = "scroll"` or `"scroll_over"` for a marquee. For two-row layouts (held top + scrolling bottom), set `bottom_text` to switch modes.

<DemoGif src="/demos/widget-gif.gif" caption="Pikachu wave gif with scroll_over text overlay" />

<TomlExample
  title="Minimal example"
  code={`[[playlist.section.widget]]
type = "gif"
path = "config/assets/my_animation.gif"`}
/>

## Options

<OptionsTable source="widgets/gif" />

## Common patterns

### Silent gif (no text)

<DemoGif
  src="/demos-pinned/gif-silent.gif"
  caption="pikachu wave centered with `pillarbox` fit, three loops"
/>

<TomlExample
  code={`[[playlist.section.widget]]
type = "gif"
path = "config/assets/pika_wave_transparent.gif"
fit = "pillarbox"
image_align = "center"
gif_loops = 3`}
/>

### Scrolling marquee over the image (`scroll_over` — text in front)

<DemoGif
  src="/demos-pinned/gif-scroll_over.gif"
  caption="pikachu pinned to the left, magenta text marquees over the canvas — magenta on yellow reads clearly where the text crosses the sprite"
/>

<TomlExample
  code={`[[playlist.section.widget]]
type = "gif"
path = "config/assets/pika_wave_transparent.gif"
fit = "pillarbox"
image_align = "left"
text = "Welcome to the show!"
text_align = "scroll_over"
font_color = [225, 48, 108]
gif_loops = 999`}
/>

### Scrolling text behind a transparent silhouette (`scroll` — text walks under the image)

<DemoGif
  src="/demos-pinned/gif-scroll_behind.gif"
  caption="text scrolls under pikachu — visible only through the transparent pillarbox area"
/>

<TomlExample
  code={`[[playlist.section.widget]]
type = "gif"
path = "config/assets/pika_wave_transparent.gif"
fit = "pillarbox"
image_align = "left"
text = "Scrolling behind Pika!"
text_align = "scroll"
font_color = [200, 200, 255]
gif_loops = 5`}
/>

### Wrap mode (seamless marquee with a separator between repeats)

<DemoGif
  src="/demos-pinned/gif-wrap.gif"
  caption="`text_wrap = true` — `BREAKING NEWS` chases itself across the canvas with a rainbow separator between repeats. Two copies of the message are on the panel at any tick, so the banner never goes blank."
/>

Setting `text_wrap = true` on a `scroll` or `scroll_over` marquee runs the text seamlessly — at every tick at least one full copy of the message is on the panel, with a configurable separator drawn between repeats. Use this for short slogans or breaking-news banners where the gap of an empty canvas between traversals breaks the rhythm.

<TomlExample
  code={`[[playlist.section.widget]]
type = "gif"
path = "config/assets/pika_wave_transparent.gif"
text = "BREAKING NEWS"
text_wrap = true
text_align = "scroll_over"
text_separator = " * "             # default: " • "
text_separator_color = "rainbow"   # default: inherit font_color`}
/>

| Field                  | Type       | Default                           | Meaning                                                                  |
| ---------------------- | ---------- | --------------------------------- | ------------------------------------------------------------------------ |
| `text_wrap`            | bool       | `false`                           | Toggle seamless wrap. Requires `text_align ∈ ("scroll", "scroll_over")`. |
| `text_separator`       | string     | `" • "` (when `text_wrap = true`) | Glyph(s) between repeats. `""` falls back to a two-space gap.            |
| `text_separator_color` | color spec | inherit `font_color`              | Color for the separator; whole-string provider (one hue per frame).      |

Notes:

- `text_loops` in wrap mode means **minimum number of cycle traversals** (one cycle = text + separator), not the off-right → off-left definition the default marquee uses.
- Two-row mode (`bottom_text` set) refuses `text_wrap` in v1.
- The separator renders in the widget's `font`; a per-separator font override isn't part of v1.

### Wrap mode (bottom row, seamless marquee)

<DemoGif
  src="/demos-pinned/gif-two_row-wrap.gif"
  caption="pikachu on the left, held magenta `BREAKING` up top, cyan `tap to subscribe` wrapping continuously on the bottom with a rainbow `*` separator"
/>

Setting `bottom_text_wrap = true` in two-row mode (when `bottom_text` is set) runs the bottom row as a seamless marquee — chasing itself across the canvas with a separator between repeats. Top row stays held at `top_align`; only the bottom row wraps. Engages even when the bottom text fits — predictable.

<TomlExample
  code={`[[playlist.section.widget]]
type = "gif"
path = "config/assets/pika_wave_transparent.gif"
top_text = "BREAKING"
top_color = [225, 48, 108]
bottom_text = "tap to subscribe"
bottom_text_wrap = true
bottom_text_separator = " * "                 # default: " • "
bottom_text_separator_color = "rainbow"       # default: inherit bottom_color`}
/>

| Field                         | Type       | Default                                  | Meaning                                                                    |
| ----------------------------- | ---------- | ---------------------------------------- | -------------------------------------------------------------------------- |
| `bottom_text_wrap`            | bool       | `false`                                  | Toggle seamless wrap on the bottom row. Requires `bottom_text` non-empty.  |
| `bottom_text_separator`       | string     | `" • "` (when `bottom_text_wrap = true`) | Glyph(s) between bottom-row repeats. `""` falls back to a two-space gap.   |
| `bottom_text_separator_color` | color spec | inherit `bottom_color`                   | Color for the bottom separator; whole-string provider (one hue per frame). |

Notes:

- `bottom_text_wrap` always wraps when set, even if the bottom text fits the canvas.
- Top row never wraps (refused by validation; no `top_text_wrap` field exists).
- The separator color inherits `bottom_color` (NOT `font_color`) — separator is part of the bottom row.
- v1's `text_wrap` stays single-row only — in two-row mode use `bottom_text_wrap`.
- `bottom_text_wrap` is only allowed in `mode = "swap"`. In `forever_scroll` / `infini_scroll` modes, validation refuses it (those modes expect widgets to terminate naturally).
- **Loop count for the bottom-row wrap:** use `text_loops` here on gif/image widgets (same field that controls single-row wrap loops). On the [`two_row`](/widgets/two_row/) widget — which is always two-row, not optionally — the equivalent field is named `bottom_text_loops` to match its bottom-prefix convention. The validator catches the cross-widget typo (rule 29) and points you at the right name.

### Two-row text overlay (set `bottom_text` to switch modes — image plays underneath)

<DemoGif
  src="/demos-pinned/gif-two_row.gif"
  caption="pikachu on the right, `@moonbunny` on top, scrolling promo on the bottom"
/>

<TomlExample
  code={`[[playlist.section.widget]]
type = "gif"
path = "config/assets/pika_wave_transparent.gif"
fit = "pillarbox"
image_align = "right"
top_text = "@moonbunny"
top_font = "5x8"
top_color = [225, 48, 108]
bottom_text = "Aerial circus · all levels welcome"
bottom_font = "5x8"
bottom_color = [255, 240, 200]
gif_loops = 3`}
/>

### Typewriter overlay (single-row only — letters type in next to the animation)

<DemoGif
  src="/demos-pinned/gif-typewriter.gif"
  caption="pikachu waves on the left while `hi friend` types in on the right with rainbow font_color"
/>

<TomlExample
  code={`[[playlist.section.widget]]
type = "gif"
path = "config/assets/pika_wave_transparent.gif"
fit = "pillarbox"
image_align = "left"
text = "hi friend"
text_align = "right"
font_color = "rainbow"
animation = "typewriter"
gif_loops = 999`}
/>

### Typewriter + rainbow border (frame the panel as the text appears)

<DemoGif
  src="/demos-pinned/gif-typewriter-border.gif"
  caption="rainbow chase border around the perimeter, white text typing in beside the waving pikachu"
/>

<TomlExample
  code={`[[playlist.section.widget]]
type = "gif"
path = "config/assets/pika_wave_transparent.gif"
fit = "pillarbox"
image_align = "left"
text = "hi friend"
text_align = "right"
font_color = [255, 255, 255]
animation = "typewriter"
border = "rainbow"
gif_loops = 999`}
/>

## Tips

### Typewriter is single-row only

`animation = "typewriter"` rejects three combinations: `bottom_text` set (two-row layout), `text_align = "scroll"` / `"scroll_over"` (cursor moves each tick), or empty `text`. Drop the conflicting setting; typewriter still composes with `font_color = "rainbow"` and `border = "rainbow"`.

### `text_align="scroll"` needs transparency

Stretch fills every pixel with image, leaving no transparent regions for the text to scroll through. Use `text_align="scroll_over"` for a fullscreen marquee, or pair `scroll` with a transparent `.gif` and `fit="pillarbox"`.

### `text_x_offset` is rejected in scroll modes

Static offsets don't compose with the per-tick scroll cursor. Set `text_x_offset = 0` or change `text_align` to `"left"` or `"right"`.

<RelatedPages slugs={["widgets/image", "widgets/two_row", "assets/emoji"]} />
