---
title: image widget
description: Single PNG/JPG (or single-frame gif) at native panel resolution, with optional text overlay.
---

import DemoGif from "../../../components/DemoGif.astro";
import TomlExample from "../../../components/TomlExample.astro";
import OptionsTable from "../../../components/OptionsTable.astro";
import DecisionRule from "../../../components/DecisionRule.astro";
import RelatedPages from "../../../components/RelatedPages.astro";

The `image` widget displays a single still image at native physical resolution — each LED maps to one pixel in the source. Use it for logos, product photos, brand artwork, or any single-frame graphic. This widget is essentially `gif` with a single frame: it shares the `gif` widget's full feature surface (fit modes, text overlay, two-row, animation, border) minus the per-frame mechanics — the only widget-specific knob is `hold_seconds` instead of `gif_loops`.

<DemoGif src="/demos/widget-image.gif" caption="Bunny png with scroll_over text overlay" />

<TomlExample
  title="Minimal example"
  code={`[[playlist.section.widget]]
type = "image"
path = "config/assets/my_logo.png"
hold_seconds = 4.0`}
/>

## Options

<OptionsTable source="widgets/image" />

## Common patterns

Static logo (no text):

<DemoGif
  src="/demos-pinned/image-static-logo.gif"
  caption="transparent moon centered with `pillarbox` fit"
/>

<TomlExample
  code={`[[playlist.section.widget]]
type = "image"
path = "config/assets/moon-transparent.png"
fit = "pillarbox"
image_align = "center"
hold_seconds = 6.0`}
/>

Image with scrolling text overlay (`scroll_over` — text in front of the image):

<DemoGif
  src="/demos-pinned/image-scroll_over.gif"
  caption="moon pinned to the left, magenta text marquees over the right side"
/>

<TomlExample
  code={`[[playlist.section.widget]]
type = "image"
path = "config/assets/moon-transparent.png"
fit = "pillarbox"
image_align = "left"
text = "Follow us on Instagram!"
text_align = "scroll_over"
hold_seconds = 5.0
font_color = [200, 100, 255]`}
/>

Scrolling text behind a transparent silhouette (`scroll` — text walks under the image; requires a PNG with transparency):

<DemoGif
  src="/demos-pinned/image-scroll_behind.gif"
  caption="text scrolls under the moon — visible only through the transparent pillarbox area"
/>

<TomlExample
  code={`[[playlist.section.widget]]
type = "image"
path = "config/assets/moon-transparent.png"
fit = "pillarbox"
image_align = "left"
text = "Scrolling behind the moon!"
text_align = "scroll"
font_color = [200, 200, 255]`}
/>

Two-row text overlay (set `bottom_text` to switch modes — image sits underneath):

<DemoGif
  src="/demos-pinned/image-two_row.gif"
  caption="moon on the right, `@moonbunny` on top, scrolling promo on the bottom"
/>

<TomlExample
  code={`[[playlist.section.widget]]
type = "image"
path = "config/assets/moon-transparent.png"
fit = "pillarbox"
image_align = "right"
top_text = "@moonbunny"
top_font = "5x8"
top_color = [225, 48, 108]
bottom_text = "Open daily 10am–8pm"
bottom_font = "5x8"
bottom_color = [255, 240, 200]
hold_seconds = 6.0`}
/>

Typewriter overlay (single-row only — letters type in next to the image):

<DemoGif
  src="/demos-pinned/image-typewriter.gif"
  caption="moon on the left, `moonbunny` types in on the right with rainbow font_color"
/>

<TomlExample
  code={`[[playlist.section.widget]]
type = "image"
path = "config/assets/moon-transparent.png"
fit = "pillarbox"
image_align = "left"
text = "moonbunny"
text_align = "right"
font_color = "rainbow"
animation = "typewriter"
hold_seconds = 5.0`}
/>

Typewriter + rainbow border (frame the panel as the text appears):

<DemoGif
  src="/demos-pinned/image-typewriter-border.gif"
  caption="rainbow chase border around the perimeter, white text typing in beside the moon"
/>

<TomlExample
  code={`[[playlist.section.widget]]
type = "image"
path = "config/assets/moon-transparent.png"
fit = "pillarbox"
image_align = "left"
text = "moonbunny"
text_align = "right"
font_color = [255, 255, 255]
animation = "typewriter"
border = "rainbow"
hold_seconds = 5.0`}
/>

## Pitfalls

<DecisionRule id="14" />

**`text_align="scroll"` + `fit="stretch"` is rejected at config-load.** The `scroll` mode exposes scrolling text through the transparent / pillarbox regions of the image. When `fit="stretch"`, the image fills the entire panel with no transparent or black regions — there is nothing for the text to show through. Use `text_align="scroll_over"` for a marquee over a fullscreen image, or use a PNG with transparency and `fit="pillarbox"`.

**`text_x_offset` is rejected with scroll modes.** The offset only applies to static text placement (`text_align = "left"` or `"right"`). With `scroll` or `scroll_over`, the marquee position is driven by the scroll logic; a constant x offset would skew the entry point in a confusing way.

**`hold_seconds` must be at least `0.05`.** Values below this minimum are rejected at config-load.

**Animated source files only show frame 0.** If you pass an animated `.gif` or `.webp` to the `image` widget, only the first frame is decoded and displayed. Use the `gif` widget for animation.

<RelatedPages slugs={["widgets/gif", "widgets/two_row", "assets/emoji"]} />
